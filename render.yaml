services:
  - type: web
    name: powerfix-case-study
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python powerfix_web.py
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
